package org.yi.happy.archive;

import static org.junit.Assert.fail;

import org.junit.Test;

public class FileStoreStreamGetMainTest {
    @Test
    public void test1() {
	/*
	 * XXX this is not strictly speaking a unit test since there are two
	 * layers of objects in use to exercise the functionality.
	 */
	// TODO
	fail("TODO");
	/*
	 * set up with no blocks in the store
	 * 
	 * launch, the two part map
	 * 
	 * check the request list
	 * 
	 * add the map block to the store
	 * 
	 * continue
	 * 
	 * check the request list
	 * 
	 * add the second part
	 * 
	 * continue
	 * 
	 * check the request list
	 * 
	 * add the first part
	 * 
	 * continue
	 * 
	 * check the output
	 */
    }
}
